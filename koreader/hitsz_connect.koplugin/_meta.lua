local _ = require("gettext")

return {
    fullname = _("HITSZ aTrust Connect"),
    description = _([[Connect to the HITSZ campus aTrust VPN from KOReader.
Credentials are read from config.lua in this plugin directory.]]),
}
